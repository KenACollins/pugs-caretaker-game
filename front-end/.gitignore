# Dependencies:
# o Coded with leading '/' means ignore this file/folder path only if found in same directory as this .gitignore file.
# o This is actually redundant, already ignored thanks to "node_modules" coded in app's server-side root folder .gitignore file.
/node_modules

# Production:
/build

# Miscellaneous:
# o Remainder consists of defaults supplied by create-react-app which may not be applicable to this app.
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
